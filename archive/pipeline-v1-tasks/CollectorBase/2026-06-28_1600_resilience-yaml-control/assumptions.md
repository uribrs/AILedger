# Assumptions

- A1 (B1): The package `RetryOptions` exposes a settable header/rate-limit-parsing strategy enum. STATUS: **REJECTED** (verified against Cymulate.Http.Package.Session 2.0.2). The DLL exposes only GETTERS `get_RateLimitDelaySource`/`get_DelaySource` (`RetryRateLimitDelaySource`/`RetryServerSuggestedDelaySource` are *reported* sources, not constructable options); no `set_`, and the `KnownIndustryHeaders`/`RetryAfterOnly` members are not in the constructable surface. ⇒ `server_delay_strategy` can only toggle `RespectRetryAfterHeader`+`ExternalizeServerSuggestedDelays` (honor vs disabled), which the SessionFactory already wires correctly. NO code change; correct the docs to this truth (no faked knob).
- A2: Leaving `SessionSpec.RateLimiter`/`CircuitBreaker` null preserves the exact Shared default (`DefaultSessionFactory.cs:~110` substitutes `?? new RateLimiterOptions{...}` / `?? new CircuitBreakerOptions{...}`). STATUS: OPEN — LEAN TRUE (read at :110). Mitigation: a build with NO new knobs must still pass 88/88 (regression gate).
- A3: `watermark_seed` string is order-comparable to the vendor's `watermark_field` values (Falcon: both ISO8601). STATUS: OPEN — true for Falcon; the seed is only the initial floor, records advance it. A vendor with a non-ISO timestamp would need a matching seed format — profile author's responsibility, not the engine's.
- A4: `RateLimiterOptions`/`TokenBucketOptions`/`CircuitBreakerOptions` field names are exactly as seen in `DefaultSessionFactory.cs` (TokenBucketCapacity, TokenBucketRefillRate, QueueLimit, AutoReplenishment; FailureThreshold, BreakDuration; RateLimiterKind.TokenBucket). STATUS: VALIDATED (read from Shared source).
