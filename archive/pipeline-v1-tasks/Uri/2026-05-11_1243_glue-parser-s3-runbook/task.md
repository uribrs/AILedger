# Glue Parser S3 Runbook

Understand how the local RunGlue project triggers AWS Glue in practice and determine what must be uploaded to S3, and where, so the `cymulate-integration-parsers` Glue workflow can run against selected parser input data.

Primary output is a planning runbook under `/Users/user/Dev/Uri/Planning`, not product-code changes.
