- A1 — VALIDATED — `System.Text.Json.Nodes.JsonObject` preserves insertion order, so the field order in `MapAsset` can be reproduced without Newtonsoft.Json. Status: validated by .NET 8 documentation and the operator's `useNewtonsoft = false` constraint.
- A2 — VALIDATED — The collector treats `cymulate_instanceId` and `cymulate_clientId` as optional and falls back to `null` instance/client fields with `"cortex-xdr"` as the finding-id prefix. Confirmed in `CortexXdrCollector.cs` (`ParseInstanceId`, `ParseClientId`, `buildFindingId`). The probe will mirror that fallback.
- A3 — VALIDATED — The probe's existing XQL execution flow (start → poll `get_query_results` → optional `get_query_results_stream`) is functionally equivalent to the collector's `FetchXqlResultsAsync`. Confirmed in `CortexXdrProbeRunner.PollQueryResultsAsync` and `PaloAltoCortexApiBase.FetchXqlResultsAsync`. Emit code can reuse it.
- A4 — OPEN — At probe-friendly query limits (default 10), the `va_endpoints` row may not include the same endpoint the `/endpoints/get_endpoint` single page returns. Asset `finding_ids` may therefore be empty even when the production collector would have populated them. This is expected at probe scale and is not a regression. To be acknowledged in `execution_notes.md`.
- A5 — VALIDATED — All asymmetries the collector itself acknowledges (embedded asset inside finding row missing `os_type/os_version/first_seen/last_seen/ip_address/mac_address/tags/fqdn-domain`; `mitigation/status/first_seen/last_seen` constant on vulnerabilities) are properties of the source data, not the mapper. The probe replicates the mapper exactly; the same asymmetries will surface and that matches the AgentService output. Documented in `CortexXdrCollector/Examples/README.md`.
