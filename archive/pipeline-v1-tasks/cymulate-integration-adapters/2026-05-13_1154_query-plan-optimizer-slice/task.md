# Query Plan Optimizer Slice

Implement the minimal Shared Query `IPlanOptimizer` stage that merges equivalent time-window execution units produced by the existing Query plan builder, while preserving native units and plan metadata.
