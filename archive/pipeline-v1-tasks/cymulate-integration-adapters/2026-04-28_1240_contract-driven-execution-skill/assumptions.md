* VALIDATED: The requested artifact is a new reusable skill, not the existing system `skill-creator` skill.
* VALIDATED: The skill should be auto-discoverable, so the default target is `/Users/user/.codex/skills`.
* VALIDATED: The skill should be named `contract-driven-execution` because it describes the workflow and follows skill naming rules.
