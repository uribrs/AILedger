- Treat this slice as composition reinforcement plus integration-test coverage. The wiring is already present in `QueryAdapterPipeline.ExecutePlanAsync` and `QueryAdapterPipeline.PublishCompletedSectionsAsync`; do not relocate it.
- Keep `QueryJobCompletedV2` construction in `QueryAdapterPipeline.PublishCompletedSectionsAsync`. The pipeline owns the executed plan and the published-section ledger; this is the correct boundary owner.
- Use the existing `IQuerySectionPublisherFactory` indirection. Do not introduce a new factory and do not couple `QueryAdapterPipeline` to a specific `QuerySectionPublisher` constructor.
- Preserve the `_sectionPublisherFactory.Create(state.Context)` call site in `ExecutePlanAsync`; do not move publisher creation into a different stage.
- Iterate `tracker.CompletedSections(cancellationToken)` via a single `await foreach` inside the pipeline so the single-consumer invariant on `QuerySectionTracker.CompletedSections` is satisfied at the wiring layer.
- Use `IAdapterExecutionContext.PublishAsync` as the publication boundary in tests; substitute a capturing `IAdapterExecutionContext` fake. Do not bypass `QuerySectionPublisher` with an ad hoc test publisher.
- Use an in-memory `IExecutionPlanStore` test fake in wiring tests so the real `QuerySectionTracker` can poll terminal state without requiring a concrete store. The store fake stays inside the test project and is not promoted to product code.
- Add wiring-focused tests in a new test file under the existing Query shared test project to keep mock-heavy composition tests (`QueryAdapterPipelineTests.cs`) separate from real-tracker integration tests. If the operator prefers consolidation, the verifier can request a merge.
- Cover three outcome paths in the wiring tests: `Succeeded` (all sections emitted, no per-query errors), `Partial` (all sections emitted, at least one `QueryResultV2.Error` set via `RecordUnitFailureAsync`), and `Failed` (at least one section never reaches terminal state in the store, so the pipeline emits `QueryJobOutcome.Failed`).
- Cover the single-consumer enforcement on `tracker.CompletedSections` at the pipeline layer: assert that one pipeline run consumes the tracker stream exactly once and that any second consumer attempt would throw the tracker's `InvalidOperationException` (use a direct second call on the tracker in a focused test, not a second pipeline run).
- Cover cancellation in the wired path: cancellation triggered while the tracker awaits store transitions must surface as `OperationCanceledException` inside the pipeline and translate to `AdapterResult.FailureResult` with `OPERATION_CANCELLED` at the pipeline boundary.
- Extend `GlobalUsings.cs` if new SDK namespaces are required by the wiring tests; do not restore per-file `using` directives in trimmed files.
- Update `Dataflow/README.md` and `Publishing/Query/README.md` to describe the wired flow and the pipeline-owned `QueryJobCompletedV2` boundary. Keep the docs short; do not duplicate the tracker contract or the publisher contract.
