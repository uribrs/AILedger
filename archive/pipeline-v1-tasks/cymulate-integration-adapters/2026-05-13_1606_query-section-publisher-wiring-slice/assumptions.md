- VALIDATED: The aggregate Query state.json lists "Wiring QuerySectionPublisher into the dataflow to consume tracker output and emit QueryJobCompletedV2" as the recommended next slice, and the section tracker handoff lists this as Recommended Next Slice #1.
- VALIDATED: `IQuerySectionPublisherFactory` is already wired into the pipeline shell. `QueryAdapterPipeline` constructor accepts it and `ExecutePlanAsync` calls `_sectionPublisherFactory.Create(state.Context)` (`QueryAdapterPipeline.cs`, line 188). The prior slice's mock-based composition tests assert the factory is invoked.
- VALIDATED: `QueryAdapterPipeline.PublishCompletedSectionsAsync` already iterates `tracker.CompletedSections(cancellationToken)` and calls `publisher.PublishSectionAsync` per emitted `SectionResultV2`, then builds and publishes `QueryJobCompletedV2` via `publisher.PublishJobCompletedAsync` after the stream completes. Counts, outcome resolution, and duplicate/unknown section guards are implemented (`QueryAdapterPipeline.cs`, lines 239-272 and 308-335).
- VALIDATED: `QueryJobCompletedV2` is built by `QueryAdapterPipeline` (not by the tracker and not by the publisher). The pipeline owns the job-end boundary because it owns the executed plan and the section-id ledger. No new pipeline-end signal needs to be introduced.
- VALIDATED: `QuerySectionTracker.CompletedSections` is single-consumer (`QuerySectionTracker.cs`, line 119-122 — `Interlocked.Exchange` guards entry, second call throws `InvalidOperationException`). The pipeline's single `await foreach` honors this.
- VALIDATED: Per-job error aggregation is the publisher path's responsibility through the section-level `QueryResultV2.Error` already set by the tracker, plus the `hasQueryErrors` flag the pipeline derives from emitted sections to set `QueryJobCompletedV2.Outcome`. The tracker does not produce a terminal envelope.
- VALIDATED: The pipeline currently has mock-based composition tests (`QueryAdapterPipelineTests.RunAsync_ComposesStagesInOrderAndPublishesJobCompletion` and four others). They prove the composition order but do not exercise the real `QuerySectionTracker` or real `QuerySectionPublisher`. The real wiring is therefore implemented but uncovered end-to-end.
- VALIDATED: `IAdapterExecutionContext.PublishAsync` is the publication surface. The publisher throws `InvalidOperationException` when `PublishResult.Success` is false (`QuerySectionPublisher.cs`, lines 39-48). Tests may use a capturing `IAdapterExecutionContext` fake that returns success.
- OPEN: Whether the operator wants the wiring tests located in the existing `QueryAdapterPipelineTests.cs` (alongside the mock-based composition tests) or in a new dedicated test file (for example `QueryAdapterPipelinePublisherWiringTests.cs`). Either is acceptable; default is a new file to keep the mock-heavy composition tests separate from the real-tracker wiring tests.
- OPEN: Whether the wiring tests should additionally cover the `QueryJobOutcome.Partial` path (a section emitted with a per-query `QueryErrorV2`) in addition to the `Succeeded` and `Failed` paths. The existing mock-based test only covers `Succeeded` and `Failed`. Recommend covering `Partial` because the tracker can produce it today and the publisher must accept it.
