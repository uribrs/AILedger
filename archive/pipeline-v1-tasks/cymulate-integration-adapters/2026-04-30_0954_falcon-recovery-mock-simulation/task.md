# Falcon Recovery Mock Simulation

Create a local runnable simulation for Falcon findings recovery behavior using the previous partial-page mock run as the concept reference.

The simulation should run the collector against a local fake CrowdStrike-compatible HTTP server, produce local runner logs and published batch files, and cover recovery behavior across month segments with multiple interruption/recovery scenarios.
