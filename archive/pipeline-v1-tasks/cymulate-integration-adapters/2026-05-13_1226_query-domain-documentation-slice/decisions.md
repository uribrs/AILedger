* Update the existing `Orchestration/Query/README.md` as the domain overview.
* Add `Orchestration/Query/Planning/README.md` for plan-builder and optimizer behavior.
* Add small `Publishing/Query/README.md` and `Recovery/Query/README.md` files beside existing Query helpers.
* Keep Dataflow topology and native coalescing described as next/future work, not current behavior.
