- VALIDATED: The `DataPipeline/` umbrella exists and `DataPipeline/Egress/`, `DataPipeline/Json/` are in place. `DataPipeline/Ingress/` is empty and ready to be populated. Source: prior task `2026-05-17_1500_shared-data-pipeline-restructure` (completed).
- VALIDATED: The universal Ingress shape is `IAsyncEnumerable<ReadOnlyMemory<byte>>`. Source: design conversation that produced this task.
- VALIDATED: `IngressStream` is a pure helper class — no interface, no base class, no options. Composition over inheritance. Source: operator decision.
- VALIDATED: Library v1 is the minimum two helpers — `ReadNdjsonLinesAsync` and `Skip`. Spool / memory-pressure / chunk-by-count are deferred until a second consumer justifies. Source: operator decision.
- VALIDATED: Cortex XDR is the first occupant. Library + Cortex refactor land in one PR, two commits. Source: operator decision (option b).
- VALIDATED: Malformed-line policy is lenient at the ingress level — bytes pass through, publisher's normalization surfaces parse errors. Preserves Cortex XDR's current behavior of not failing a 50k-row drain over a single bad line. Source: operator decision (defaulted, documented as decision in `decisions.md`).
- VALIDATED: `Skip` takes `long`. Future-proof against time-based adapters, no cost. Source: operator decision (defaulted, documented as decision in `decisions.md`).
- VALIDATED (risk-accepted): `cve_id` uniqueness in `va_cves` rows. Operator decision 2026-05-17: do not gate on vendor confirmation. If duplicates exist and resume-by-index drifts on tie-order across runs, upstream dedup is the fallback. Repo-internal evidence (plural array columns `affected_hosts`/`affected_products` on a single row, original author's reliance on `cve_id` for ordering, code-review-added tie-breaker rather than incident-driven) is consistent with uniqueness; medium confidence in favor. Risk surface and supporting evidence retained in `research/cve-id-uniqueness.md`. Commit 2 is unblocked.
- OPEN: Exact verification command set. Candidates:
  - `dotnet build src/Cymulate.Integration.Adapters/Collectors/CortexXdrCollector/Cymulate.Integration.Adapters.Collectors.CortexXdrCollector.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false`
  - `dotnet test src/Cymulate.Integration.Adapters/UnitTests/Collectors/Cymulate.Integration.Adapters.Collectors.CortexXdrCollector.Test/Cymulate.Integration.Adapters.Collectors.CortexXdrCollector.Test.csproj --no-restore --disable-build-servers -p:UseSharedCompilation=false`
  - Plus the Shared project's targeted test command for the new Ingress unit tests once their location is confirmed.
  Orchestrator/executor confirms against repo conventions before running.
- OPEN: Location of `DataPipeline.Ingress` unit tests. The repo has Shared test projects under `src/Cymulate.Integration.Adapters/UnitTests/Shared/...`. Executor selects the existing test project that hosts `DataPipeline.Egress` / `DataPipeline.Json` tests today, or creates a sibling test project if the layout requires it. Match the existing convention.
- OPEN: Whether `CortexXdrXqlClient` has any caller other than `CortexXdrFindingsFlow`. Pre-flight grep before changing the signature. Earlier review noted it as the only caller, but verify under the new layout in case the rename moved references.
