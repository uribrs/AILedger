# Disable batch-scoped storage opt-ins

Remove the production `true` opt-ins for batch-scoped storage from all collectors currently enabling them, align affected tests and documentation comments, and verify the collectors retain a flat storage layout by default.
