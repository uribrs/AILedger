# Query Native Coalescer Slice

Implement the minimal Shared Query `INativeCoalescer` stage that coalesces equivalent native execution units in an SDK `ExecutionPlan`, while preserving time-window units and plan metadata.
