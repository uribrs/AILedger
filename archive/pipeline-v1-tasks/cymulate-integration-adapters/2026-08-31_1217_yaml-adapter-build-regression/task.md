# Task

Identify exactly when the YamlAdapter build regression began and which recent pull request introduced it, using Git and package evidence from both `cymulate-integration-adapters` and `yaml-adapter-engine`.
