# Falcon Cursor Recovery Observability

Fix Falcon findings recovery cursor correctness and add operational visibility for current cursor/date progress.

The work should preserve CrowdStrike cursor invariants, prevent repeated cursor loops, extend local mock simulation coverage, and add logs that help determine which date and approximate part of a day the collector is processing.
