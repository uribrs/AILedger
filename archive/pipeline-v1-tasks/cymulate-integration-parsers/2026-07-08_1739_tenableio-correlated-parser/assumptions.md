# Assumptions

- **A1 — VALIDATED**: Input routing is file-sniffed by `libs/packages/utilities/input_resolver.py` (:77-107): `assets*.json` + `findings*.json` present → `input_mode="split"`; only `findings*.json` → `input_mode="hydrated"`. New collector emits only `findings_*.json` → correlated envelopes arrive as HYDRATED; old collectors keep resolving to split.
- **A2 — VALIDATED**: Engine `process_hook` is all-or-nothing per spec (`yaml_parser.py:214-219`), but the hook receives the parser instance and can drive the declarative path itself → a mode-router hook (split → `delegate_to_original`; hydrated → declarative) is feasible. `skip_post_shaping` asymmetry must be handled: delegate returns fully-shaped frames; declarative frames need `post_process` shaping (`yaml_parser.py:468-486`).
- **A3 — VALIDATED**: Multi-chunk hosts (chunk 0..N per host) mean random per-row stamping mints duplicate assets. Correct idiom: `assets.dedup_key` (prefer `host.id`) + `findings.link_strategy: link_to_asset` joining finding `asset.uuid` ↔ asset-lane `aid`; engine dedups assets BEFORE the join lookup (`yaml_parser.py:398-457`), so all chunks converge on one `asset_id`.
- **A4 — VALIDATED**: Zero-vuln assets survive structurally: assets lane reads every envelope row; only the findings explode drops empty arrays (`yaml_parser.py:346-363`).
- **A5 — VALIDATED**: Declarative hydrated specs read `options.findings_file_path` with the resolver's multi-file glob (`yaml_parser.py:229-243`) — same load path as SC.
- **A6 — VALIDATED (orthogonality to verify at execution)**: master @2bc1422 includes CA-74751 — create-entities envelope carries `batchId` as sole batch marker (isFinal/isBatch dropped); batch-key parse mode exists platform-side. Expected orthogonal to this spec; execution confirms nothing in it constrains the spec.
- **A7 — VALIDATED (both generations live)**: legacy hydrated is actively harness-tested (`test_files/tenable/assets_and_findings/findings.json` is the legacy combined shape with golden snapshots) → it MUST keep working. Resolution: shape-sniff inside the hydrated branch (`{host, findings, isLastChunk}` columns + `host` is a struct → correlated; else legacy path untouched). Legacy snapshots double as the regression proof.
- **A8 — VALIDATED (via design pivot, see D7)**: the parity baseline is the split path's own machinery — `_build_asset_envelope_df` (11-field asset envelope incl. `acr_score_v3` ← `ratings.acr.score`, singular projections, `tag_*` normalization) + dynamic per-sub-field plugin/root walks + uuid dedup + LEFT-join. The declarative YAML vocabulary cannot reproduce the dynamic per-sub-field plugin flatten (engine `dynamic` mode walks top-level columns and `to_json`s the plugin struct wholesale; an explicit map would freeze ~50 drifting plugin keys). Exact parity is achieved by construction: correlated input derives the two split lanes and reuses the split path verbatim. Real host struct confirmed: `ratings.acr.score` present (206/206 sampled), finding root keys incl. `last_fixed`/`recast_*`/`resurfaced_date` covered by the dynamic root walk.
- **A9 — VALIDATED**: two-tier fixtures. Unit test builds synthetic envelopes inline (sibling style of the split test): fabricated multi-chunk host (real run had ZERO multi-chunk envelopes — no host exceeded the 2,000-findings cap), zero-findings host, normal host, all-zero-findings batch, legacy-sniff regression. Harness fixture is a sanitized real-data cut (`findings_correlated.json`, 4 envelopes/3 hosts incl. fabricated 2-chunk split of a real host) + golden snapshots; sanitization sweep verified no customer identifiers remain.
