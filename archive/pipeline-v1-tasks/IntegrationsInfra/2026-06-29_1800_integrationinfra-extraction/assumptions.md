# Assumptions

- **A1 (OPEN) — make-or-break:** A clean single-entry capability bank is feasible — i.e. a high enough fraction of adapter substrate usage is capability/contract-shaped (not raw-internal) that a small capability surface can cover ~100% of real usage. Resolved by the USAGE CENSUS to VALIDATED or REJECTED. A REJECT is a go/no-go the user must see.
- **A2 (OPEN):** Each of the 15 areas can be classified complete vs hybrid/incomplete. Hybrids are rework/drop/defer candidates, not contract material.
- **A3 (OPEN):** Glossary fully evicts to bus/BE ownership despite 122 import sites + the native-collector self-identity usage (the wrinkle making it M–L, not trivial).
- **A4 (OPEN):** The runtime-plugin (`deps.json` closure) deploy model is unaffected by splitting Shared into facade + mechanics packages (both ship inside the adapter's closure). To confirm during analysis.
