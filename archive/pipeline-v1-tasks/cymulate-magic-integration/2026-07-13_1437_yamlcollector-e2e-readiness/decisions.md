# Decisions

- Use tenable.io as the concrete trace vehicle end to end (user-specified).
- Assessment baseline is ADR-0001/0002/0003 in the magic repo — the locked direction, not the
  retired peer-bridge model.
- Deliverable is analysis + diagram only; any fixes found are reported, not applied.
- Diagram delivered as a rendered artifact (mermaid/HTML) plus source in the task directory.
