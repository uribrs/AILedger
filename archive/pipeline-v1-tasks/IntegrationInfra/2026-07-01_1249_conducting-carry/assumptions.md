# Assumptions

- A1 — `DependencyInjection/CollectorCommonDependencies.cs` is in-scope Conducting infra. Status: VALIDATED (S1). Deps only DefaultSessionProvider (Conversation) + SDK + MS.DI/Logging; no Orchestration-internal or Emission drag. → `Conducting/DependencyInjection/` (ns `...Conducting.DependencyInjection`).
- A2 — `Contracts/Handlers/BaseFlowHandler.cs` placement. Status: RESOLVED = EXCLUDE (S1). It + `ITopicHandler` (same source folder) are indicator-side handler-template contracts with ZERO references from the 38 collector-pipeline files and not carried anywhere. Not Conducting scope. FOLLOW-UP: these indicator handler contracts have no home in the current 7-concern decomposition — separate operator decision.
- A3 — `UnknownFlowRetryPolicy.CreatePipeline` reconstructs verbatim against the Kernel `UnknownFlowRetryClassification` residue. Status: VALIDATED (S1). CreatePipeline uses MaxRetries/RetryDelays/IsUnknownRetryCandidate; predicate uses HttpTransportFailureClassifier — all in Kernel.Transport. FaultGovernance `UnknownFlowRetryPolicy` = CreatePipeline + thin forwarders (MaxAttempts, IsUnknownRetryCandidate); logic single-homed in Kernel, no duplication.
- A4 — Polly package reference resolves. Status: VALIDATED (S1) — already referenced in FaultGovernance. DefensiveToolkit NOT needed in Conducting core (no such usings in scope).
- A5 — The two run-templates + all hard behavioral invariants survive a namespace-only move with ZERO logic change. Status: VALIDATED (S6 build clean + S8 invariant tests green: partial-success-wins + cancellation-no-publish confirmed against the real runner). No logic changed.
- A6 — Scope = 38 source `Orchestration/` files + `CollectorCommonDependencies` + the FaultGovernance `CreatePipeline` addition; `BaseFlowHandler`/`ITopicHandler` EXCLUDED. Status: VALIDATED (S1).
- A7 — DAG correction (NEW, S1): Conducting depends on **Job** (AdapterTopics) and **Kernel** in addition to Conversation/FaultGovernance/Reporting/Envelopes.Common/SDK. Still NOT Emission; still acyclic. Status: VALIDATED — update README charter.
