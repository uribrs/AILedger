# Assumptions

- **A1 — REJECTED (does not apply).** Resolved by W1 (`research/engine-map-and-change-plan.md`): the AgentService collector has **no checkpoint and no resume** — it waits for export FINISHED then uploads. The C1 mid-chunk-resume duplication path is therefore structurally impossible here, so there is nothing to port. The only C1-adjacent improvement is to stop swallowing non-cancellation exceptions (`:416-420`), which currently let a truncated run report success.
- **A2 — OPEN.** Whether STG runs the AgentService collector or the integration adapter is unconfirmed. The user is treating the agent service as an STG-tested target, so proceed assuming the agent service is a live target worth mirroring.
- **A3 — VALIDATED.** Resolved by W1: egress is `ICybiBatchUploader` writing `<prefix>_{n:D3}.json` into backend folder `cybi/{instanceId}`; lanes co-locate by sharing that folder and differing only by filename prefix (`assets_` vs `findings_`). No checkpoint/resume. → `decisions.md`.
- **A4 — VALIDATED.** Resolved by W1: dispatch is `CollectAssetsAsync` (currently a no-op stub) + `CollectFindingsAsync`. The combined-run pattern already exists in the sibling **FalconCollector** (`CollectFindingsAsync` runs assets stage then findings stage into co-located feeds) — the canonical in-repo template to mirror. → `decisions.md`.
- **A5 — VALIDATED.** Endpoints/filters/shape are settled by the adapter (validated against live data): `/assets/export` `last_assessed` 30d; `/vulns/export` `since` + `state=[OPEN,REOPENED]`; all severities incl. info; `/assets/{id}` enrichment removed; ACR from `ratings.acr.score`. Source: adapter `Documentation/01-collection-strategy.md` + live run. → see `decisions.md`.
- **A6 — VALIDATED.** Co-located dual-lane output is what the split parser requires (assets-only lane hard-throws in the parser's input resolver). Source: adapter `Documentation/02-decision-making.md` (D5). → see `decisions.md`.
