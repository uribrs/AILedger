# Assumptions

- A1 — OPEN — Opt-in key name/location in the action payload. Candidate: a key inside `IntegrationInstanceJson` (e.g. `isBatch` already exists on the instance's setting server-side) or a dedicated key in `RawParameters`; read executor-side by `CybiActionManager` (lenient `JObject` peek on the raw string — no shared DTO change). Execution may implement against a named placeholder key; final name coordinated with the server half.
- A2 — OPEN — CyAgentServer preserves a `/`-containing `fileName` into the S3 key and relays the new body fields (`metadata`, `sequenceId`, `itemCount`) into the `collectors.progress` payload. NOT verifiable on this machine (repo absent). Out of this task's scope; the agent side is built to final shape regardless. Probe plan exists (S3 object location + consumer's "Collection progress received" Elastic log line).
- A3 — VALIDATED — Agent holds all consumer-required metadata fields: `IntegrationInstanceJson` is the full `CybiClientIntegrationInstance` doc (`_id`, `id`, `clientID`, `integrationSettingId`, `integrationSettingFlowId`, `clientIntegrationFlowId`, `clientIntegrationId`). Verified against cySharedDBModels model + `CybiIntegrationAction.enrichCredentialsForBatchUpload` (reads `_id` today).
- A4 — VALIDATED — Consumer contract (merged to master): reads `payload.metadata.storageUrl` + `payload.metadata.instanceBatchId` (top-level fallback), `sequenceId` monotonic guard, gates on `micro-batching` + `integrationSetting.isBatch`, upserts on unique `id`, triggers Glue `{env}-epc` with `BATCH_KEY=ZIP_FILE_KEY=scoped path`, `IS_BATCH=true`, `BATCH_ID=instanceBatchId`. Verified by reading `connector-manager.service.ts`.
- A5 — VALIDATED — Consumer triggers Glue on EVERY progress event carrying `instanceBatchId` (no once-per-insert guard) → announce-once-per-folder is mandatory on the agent side. Verified: `handleStreamingBatch` upserts `$setOnInsert` then calls `triggerGlueBatch` unconditionally.
- A6 — OPEN — Default granularity value (k flushes / byte threshold per folder) — needs an operator/server-side decision before enablement; implementation makes it a parameter, default value provisional.
- A7 — VALIDATED — `CybiBatchUploader` is a DI singleton in the executor process; one executor = one action run; uploads within a run may be concurrent. Verified: `ServiceCollectionExtensions.cs:564`, Hertz log thread ids, existing `Interlocked` counter.
