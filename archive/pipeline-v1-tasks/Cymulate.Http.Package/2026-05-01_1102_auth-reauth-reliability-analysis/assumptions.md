* VALIDATED: "Long-running processes" means continuous or recurring use over multiple days to weeks without recreating application credentials.
* VALIDATED: Consumer-side mitigations are acceptable even when the package itself cannot guarantee provider-specific token lifetime behavior.
* VALIDATED: Repository unit tests are useful for behavioral intent, but code is the source of truth for current mechanism analysis.
