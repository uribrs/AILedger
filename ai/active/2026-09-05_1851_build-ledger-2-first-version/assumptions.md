# Assumptions

- A1 VALIDATED — actor: verifier — The original request asks for the first version built from the local Ledger, and the signed contract adopts the bounded v2.1 first slice while explicitly excluding later swarms, automatic execution, generic policy, hard filesystem enforcement, and archive/TTL mechanics. Evidence: `task.md`, `prompt_contract.md` Goal and Success Criteria, `constraints.md`, and `AILedger_2_Design_Dossier_v2.1.docx` §16.
- A2 VALIDATED — actor: verifier — Codex CLI `0.150.0-alpha.8` and Claude Code `2.1.261` are installed locally, and their scripted launch/resume, JSONL output, session identity, working-directory, prompt, and bounded-permission surfaces agree with official guidance, current local help, deterministic tests, and authenticated new/resume runs. Evidence: [OpenAI non-interactive mode](https://learn.chatgpt.com/docs/non-interactive-mode), [Claude programmatic mode](https://code.claude.com/docs/en/headless), `research/provider-cli-launch-contracts.md`, `execution_notes.md` CR1/CR2 and CL6/CL7, and persisted Claude session `487b780d-c150-4c6b-aaef-7552aa592d1e`.
- A3 VALIDATED — actor: verifier — All task mutation enters through `IGovernedTaskService.ExecuteAsync`; provider launch records start/completion through that service while child processes receive context and return through `IAgentAdapter`, and concurrent service instances serialize the production command path. Evidence: `src/AILedger.Core/Contracts/PersistenceContracts.cs:11`, `src/AILedger.Cli/CliApplication.cs:261`, `tests/AILedger.Tests/Storage/ConcurrencyTests.cs::R3_ConcurrentStartsAllowOnlyOneActiveRun`, and the persisted CR1/CR2/CL4/CL5 smoke-run events cited in `execution_notes.md`.
- A4 VALIDATED — actor: verifier — Eight cognitive files exactly match Git object bytes from source commit `5bdb62717f8da0b33075b704b519a9e80a4e8490`, while authorization, lifecycle, invalidation, isolation, run, and persistence rules live separately in code and are mapped without modifying the copied text. Evidence: independent manifest/source SHA-256 check on 2026-09-05, `cognitive/manifest.json`, `src/AILedger.Core/Domain/`, and `docs/architecture.md#rule-to-mechanism-mapping`.
- A5 VALIDATED — actor: verifier — The local file store replays append-only events as authority, repairs stale materialized state and Markdown views, and serializes concurrent mutation through process and file locks; the full storage suite and CLI persistence smoke passed. Evidence: `src/AILedger.Storage/FileGovernedTaskService.cs`, `tests/AILedger.Tests/Storage/RecoveryTests.cs::R1_ReopenReplaysEventsWhenMaterializedStateIsStale`, `tests/AILedger.Tests/Storage/ConcurrencyTests.cs`, and verifier CLI smoke workspace `/private/tmp/ailedger-verifier.QSdhFM`.
- A6 NEVER-TESTED — actor: verifier — The repository still has no `HEAD`, and the no-commit constraint correctly prevented establishing the initial commit during execution. Evidence: `git rev-parse --verify HEAD` failed on 2026-09-05 and `constraints.md` says not to commit without explicit request. Risk: the completed files remain unversioned until the operator authorizes and creates the first commit.
- A7 VALIDATED — actor: verifier — Despite the three missing reference files, the researcher produced a decision-grade provider contract that named exact commands, permissions, protocol semantics, failure conditions, and verify-first checks; the resulting adapters, tests, and authenticated new/resume smokes substantively exercised it. Evidence: `research/provider-cli-launch-contracts.md`, `tests/AILedger.Tests/Providers/`, and `execution_notes.md#integrated-verification`.

## Prior Art

No prior art found for tags: ailedger, governance, multi-agent, event-sourcing.
