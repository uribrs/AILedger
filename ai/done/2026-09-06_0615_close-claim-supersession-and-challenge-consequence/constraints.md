- Scope is the two gaps in `task.md` plus the named enum hygiene. Do not open a third.
- Do not collapse or refactor the `CommandHandler` / `TaskTransitionValidator` rule duplication. That is the next task.
- Every rule must be expressed in both `CommandHandler` and `TaskTransitionValidator`, and the two copies must agree.
- Every new or changed event type must be registered in the `TaskTransitionValidator.Validate` switch; an unregistered type makes a task permanently unreadable.
- Do not reorder or rename `GovernedTaskState` properties; `state.json` is byte-compared against a fresh replay.
- Do not modify `cognitive/` or `src/AILedger.Providers`.
- Do not weaken existing authorization, causal invalidation, or code-reviewer context isolation.
- This is a personal POC. Prefer the simplest mechanism that satisfies a success criterion; add no hardening or abstraction beyond it.
- A verifier or code-reviewer finding is a repair only if it violates a success criterion or stop condition in this contract. Everything else is recorded in `execution_notes.md` under `## Backlog` and is not fixed here.
- At most two verifier rounds and two code-reviewer rounds. If a third would be needed, stop and report.
- Do not commit or push unless explicitly requested.
